version: "2"
run:
  timeout: 5m
linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - exhaustive
    - gosec
    - misspell
    - nilerr
    - nilnesserr
    - perfsprint
    - prealloc
    - testifylint
    - unconvert
formatters:
  enable:
    - gofmt
    - goimports
